# myls-dumper

tampermonkey script for Brightspace/D2L-based LMS course sites walks content module tree (`#D2L_LE_Content_TreeBrowser`) which is rendered fully on the client, fetches each module topic list via ModuleDetailsPartial, checks if its file-backed and if it is, downloads it

will probably not work for all course content layouts